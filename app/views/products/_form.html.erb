<%= simple_form_for @product do |f| %>
  <%= f.input :name %>
  <%= f.input :category_id, collection: @category %>
  <%= f.input :price %>
  <%= f.input :quantity %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, class:"btn-outline-secondary" %>
<% end %>
